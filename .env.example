# Cloud LLM lane (P2, optional) — copy to .env, never committed.
# Point at any OpenAI-compatible endpoint (Aperture over the tailnet, OpenRouter, ...).
LECTOR_CLOUD_BASE_URL=
LECTOR_CLOUD_API_KEY=
LECTOR_CLOUD_MODEL=
